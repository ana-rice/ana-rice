# Ana Rice

## Setup Backend

- [Install `pipx`](https://pipx.pypa.io/latest/installation/).
- Install `poetry`:

```bash
pipx install poetry
```

- Initialize Python shell.

```bash
cd backend
poetry shell
```

- Add dependencies:

```bash
poetry add [dependency]
```
